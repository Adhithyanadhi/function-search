{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run Extension (proposed APIs)",
      "type": "extensionHost",
      "request": "launch",
      "runtimeExecutable": "${execPath}",
      "args": [
        "--extensionDevelopmentPath=${workspaceFolder}",
        "--enable-proposed-api",
        "AmbitiousCoder.function-name-search"
      ]
    }
  ]
}
